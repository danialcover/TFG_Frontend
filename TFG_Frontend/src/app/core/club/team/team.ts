export class Team {
    id: number;
    name: string;
    club: number;

    constructor(id: number, name: string, club: number) {
        this.id = id;
        this.name = name;
        this.club = club;
    }
}
